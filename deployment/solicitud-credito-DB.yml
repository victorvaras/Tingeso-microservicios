apiVersion: v1
kind: PersistentVolume
metadata:
  name: solicitud-credito-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/solicitud-credito-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solicitud-credito-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: solicitud-credito-db-deployment
  labels:
    app: solicitud-credito-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: solicitud-credito-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: solicitud-credito-db-deployment
        tier: database
    spec:
      containers:
        - name: solicitud-credito-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_SOLICITUD-CREDITO
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_SOLICITUD-CREDITO
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: solicitud-credito-db-data
      volumes:
        - name: solicitud-credito-db-data
          persistentVolumeClaim:
            claimName: solicitud-credito-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: solicitud-credito-db-service
  labels:
    app: solicitud-credito-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: solicitud-credito-db-deployment
    tier: database
  type: LoadBalancer