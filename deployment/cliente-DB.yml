apiVersion: v1
kind: PersistentVolume
metadata:
  name: cliente-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/cliente-db-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cliente-db-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cliente-db-deployment
  labels:
    app: cliente-db-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: cliente-db-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cliente-db-deployment
        tier: database
    spec:
      containers:
        - name: cliente-db
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_CLIENTE
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_CLIENTE
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: cliente-db-data
      volumes:
        - name: cliente-db-data
          persistentVolumeClaim:
            claimName: cliente-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cliente-db-service
  labels:
    app: cliente-db-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: cliente-db-deployment
    tier: database
  type: LoadBalancer